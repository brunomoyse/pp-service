// Seating-specific input types will be moved here from crate::gql::types
// during final cleanup. For now, types are imported from crate::gql::types
// to avoid duplicate GraphQL type registrations.
